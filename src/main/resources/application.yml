server:
  port: 8090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/book?serverTimezone=UTC&allowMultiQueries=true
    username: root
    password: 1231
    driver-class-name: com.mysql.cj.jdbc.Driver

# 配置 MyBatis
mybatis:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.sjt.lib.book.domain

